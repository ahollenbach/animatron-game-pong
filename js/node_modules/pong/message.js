const ClientMessage = {
	PADDLE_LOCATION : "paddle_location"
};

const ServerMessage = {
	GAME_INITIALIZATION : "game_initialization",
	PADDLE_LOCATION : "paddle_location"
};

// Check if on server
if (exports !== undefined) {
	exports.ClentMessage = ClientMessage;
	exports.ServerMessage = ServerMessage;
};